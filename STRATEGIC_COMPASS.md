# STRATEGIC COMPASS — Longcycle 的长期航向

> 这是一个**有界长期核心**。它只保存项目使命和决策方向，不保存当前行业、任务、数字、CI、分支或开发历史。

## 使命

> **历史本身就是分析。**

Longcycle 要成为一个**证据可追溯、时间语义真实、可以从历史当时重新进入产业世界的长期产业记忆系统**。

它必须同时保存：

- **Reality**：当时真实发生了什么；
- **Expectation / Judgment**：当时的人看到了什么、相信什么、预期什么、为什么；
- **Outcome**：后来实际发生了什么，以及预期如何被验证、修订或击穿。

真正的产品价值不是“拥有很多数据”，而是能够在一个过去时点只使用当时已知的信息进行 **point-in-time replay**，再与后来的 Outcome 比较。长期、真实、可比的历史本身应足以让很多风险和机会通过常识显现，而不是依赖黑箱预测。

## 项目成功的第一性标准

Longcycle 的终局不是：数据库更大、Agent 更多、RAG 更漂亮、搜索结果更多、Schema 更抽象、报告更自动化。

真正的验收是：

1. 重要历史能够追溯到原始证据；
2. Reality、当时的 Expectation 与后来 Outcome 不被后见之明混写；
3. 时间、口径、规格和状态足够真实，可以跨周期比较；
4. 可以站在过去重新看到一个仍然不确定的未来；
5. 从真实行业中证明的方法能够迁移，而不是永远停留在某个行业特例。

如果长期工程越来越复杂，却不能更接近这些能力，就是偏航。

## 战略层级

每个 Agent 都必须区分四层：

```text
长期：项目最终使命
  ↓
中期：当前要证明的产品能力 / benchmark
  ↓
短期：当前阶段要跨过的里程碑
  ↓
执行：具体代码、数据、研究、测试
```

中期和短期目标由 `.longcycle/handoff/current.json` 保存，本文件不复制。

任何执行工作必须能解释它怎样推进短期目标、短期目标怎样推进中期目标、中期目标怎样证明长期使命。解释不了时，默认重新排序，而不是继续把局部工作做深。

## 工具永远不是目的

数据库、Crawler、RAG、Prompt、Memory Atlas、Agent orchestration、CI、Handoff、UI 和具体行业都只是手段或证明环境。

不能因为某个工具容易优化、容易量化或很有技术趣味，就让它取代产品使命。

## 长期核心的修改规则

本文件只在以下情况下修改：

1. 用户明确改变最终产品方向；
2. 多个真实 benchmark 证明核心假设需要调整；
3. 有足够强、可审计的新证据要求改变航向。

一次行业特例、一次实现困难、一个新框架或一个 Agent 的个人偏好，都没有资格修改长期使命。

新增内容必须替换、压缩或重构已有表达，**不能无限追加**。具体行业经验、阶段决策和开发历史必须留在各自 context / methodology / devlog 中。

## Strategic Alignment Gate

开始一段重要工作前，只需要回答四个问题：

1. Longcycle 最终要让用户获得什么能力？
2. 当前中期大目标是什么？
3. 当前短期任务为什么是通往中期目标的必要或高价值一步？
4. 完成它以后，下一层更大的动作是什么？

答不清楚，就先找回航向，再继续执行。

> **先确认航向，再拧螺丝。**
