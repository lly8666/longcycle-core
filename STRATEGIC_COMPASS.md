# STRATEGIC COMPASS — Longcycle 的舵

> 这不是当前 TODO，也不是架构说明。它保存的是跨无数次会话、模型和 Agent 都不能丢掉的**大方向**。
>
> 当前任务可以不断变化，但航向不能因为局部工作越来越细而消失。

## 0. 创始意图 / 用户原话

> “把整个行业相关的最关键和真实的历史保存下来，拉长时间去看，其实不用太多分析也能用简单常识分析出当下的风险与机遇”

> “缺的是人站在当时的判断和预期。”

> “高级大模型关于产业链几乎所有内容从记忆里输出来，比哪怕高级agent去互联网搜历史信息更有价值；历史信息让低级agent拿着高级agent的记忆去逐条印证。”

> “大海航行靠舵手……让他们深刻记住最开始的大逻辑和方向，永远不要偏离航向，永远知道我下一步大的方向是什么，避免多个agent过后钻入牛角尖。”

这些话是行为约束，不是背景介绍。

## 1. 一句话使命

**Longcycle 要成为一个可以把行业历史重新活一遍的、证据可追溯的产业记忆系统。**

它保存的不只是后来知道的“事实答案”，还要保存当时的人看见了什么、相信什么、为什么这么相信、采取了什么行动，以及后来现实怎样验证或击穿了这些判断。

核心命题：

> **历史本身就是分析。**

把足够长、足够真实、足够可比、足够 point-in-time 的历史放在一起，很多风险与机会不需要复杂预测模型就会自己显现。

## 2. 终局不是“数据库建好了”，而是历史能够被重放

项目真正成功的验收不是：

- 有很多表；
- 有很多 Agent；
- 有漂亮的 RAG；
- 有大量搜索结果；
- Memory Lead 数量很大；
- CI 很漂亮；
- 能自动写行业报告。

真正的第一性验收是：

> 给定一个历史时点，例如 2022-06-30，Longcycle 能只用当时已经可知的材料，重建一个理性产业参与者所能看到的 Reality、leading indicators、管理层 guidance、机构预期、主要争论、项目时间表、供需约束、资本开支和未决风险；然后再逐步揭示后来发生的 Outcome，看到哪些判断为何成立或失败。

如果系统长期做了很多工程，但仍不能完成这种 replay，说明项目偏航了。

## 3. 战略层级：任何 Agent 都必须知道自己在哪一层

```text
H0 — 终局使命
可回放、可追溯、跨周期的产业记忆

↓

H1 — 第一证明
用锂电产业链完成一个真实端到端 benchmark：
Reality + Expectation + Outcome + point-in-time replay

↓

H2 — 当前战略路线
历史：Memory-first, Evidence-final
当下：Source-first, Archive-now

↓

H3 — 当前阶段
先榨尽高级模型的历史目录 → seal → 高级模型先自证/搜索 → 低级 Agent 逐条证据化

↓

H4 — 当前任务
例如 Memory Atlas batch3 novelty decay、compact index、某个 schema 修正

↓

H5 — 局部实现
某个字段、测试、CI、SQL、Prompt、适配器
```

**H5 必须服务 H4，H4 必须服务 H3，H3 必须服务 H2，H2 必须推进 H1，H1 必须证明 H0。**

如果无法解释这条因果链，局部工作即使技术上正确，也不应自动继续。

## 4. 锂电是“证明场”，不是产品终点

锂电项目的意义不是把锂电研究做到无限精细，而是用一个用户熟悉、产业链复杂、周期明显、资料丰富但历史检索困难的行业，把 Longcycle 的核心方法逼出来。

锂电 benchmark 至少要证明：

1. 关键历史事实能够被长期保存并追溯到原始证据；
2. 当时预期和理由能够与后来事实分开保存；
3. 项目、产能、价格、库存、需求和技术能够以真正可比的语义形成轨迹；
4. 可以站在过去某一天做 no-lookahead replay；
5. 可以把当时的预期与后来 Outcome 对起来；
6. 简单常识和长期比较已经足以暴露一部分周期风险/机会；
7. 这套方法可以迁移到第二个行业，而不是锂电专用手工系统。

在这些尚未被证明前，优先级通常高于“建设更通用、更漂亮的平台”。

## 5. 两条永久主线

### A. 历史恢复：Memory-first, Evidence-final

旧互联网天然残缺，宽泛搜索会漏掉大量不显著但关键的历史。

```text
高级模型 Blind Memory Exhaustion
→ Memory Atlas
→ saturation / seal
→ 高级模型 Self Verification / Search Discovery
→ 低级 Agent Claim-scoped Evidence Engineering
→ Archive 原始资料
→ Reality / Expectation / Outcome
→ Point-in-time Replay
```

高级模型的价值之一，是充当“被压缩的人类公开历史索引”。模型记忆不是证据，但它能告诉系统**还应该去找什么**。

### B. 当下采集：Source-first, Archive-now

今天仍容易找到的原始公告、统计、IR、项目批复和行业资料，应当今天就保存，避免几年后再抢救。

```text
Source Watchlist
→ 主动检查
→ 原文/版本归档
→ Reality / Judgment extraction
→ revision tracking
```

历史恢复和当前采集不能互相替代。

## 6. 工具与目标必须严格区分

| 东西 | 它是什么 | 它不是什么 |
| --- | --- | --- |
| Memory Atlas | 历史目录/缺口探测器 | 最终事实库 |
| 高级模型 | 历史线索生成器、关联器、自证研究员 | 权威事实来源 |
| 低级 Agent | 证据工程执行者 | 自主行业结论生成器 |
| Web 搜索 | 找原始证据的手段 | 真值裁判 |
| PostgreSQL / Schema | 保存时间、语义、证据和版本的基础设施 | 产品价值本身 |
| CI / Tests | 防止实现和研究契约漂移 | 当前研究主线 |
| Handoff | 保持多 Agent 接力不失忆 | 一个值得无限优化的独立产品 |
| 锂电 | 第一真实 benchmark | Longcycle 唯一行业 |
| 报告/UI | 最终使用方式之一 | 系统的知识基础本身 |

## 7. 防止“钻牛角尖”的战略反模式

出现以下行为时，Agent 必须主动检查是否偏航：

- 因为 crawler / RAG / agent framework 很容易扩展，就开始做通用平台，而锂电 replay 还没有跑通；
- 为了让 Memory Lead 数字更大而无限召回，却不测 novelty decay、不 seal、不进入证据化；
- 为了 schema 完美而持续增加抽象层，但没有真实资料要求这些抽象；
- 在一个材料、公司或项目上无限深挖，已经不能显著改善整个周期 replay；
- 把修 Ruff、格式、CI 美化变成主要开发工作，而 correctness 已经足够支撑主线；
- 因为搜索方便而提前污染 blind memory；
- 因为搜索不到而删除历史线索；
- 把“找到很多网页”误认为“历史已经恢复完整”；
- 只保存 Reality，忘记保存当时的 Expectation、理由和 revision；
- 只做历史抢救，忘记今天的 source-first/archive-now；
- 把锂电特例硬编码成永久架构，而没有追问能否迁移到下一行业；
- 把 handoff、agent orchestration、数据库或 Prompt 优化本身当成最终产品。

## 8. 每个重大任务前必须过 Strategic Alignment Gate

新会话、重大 phase 切换、或连续一个 coherent batch 的工程工作开始前，Agent 必须能回答：

1. **Longcycle 最终想证明什么？**
2. **锂电 benchmark 的第一性验收是什么？**
3. **当前阶段在历史恢复总链条里的位置是什么？**
4. **当前任务如何让我们更接近 Reality / Expectation / Outcome replay？**
5. **如果不做这个任务，主航道会被阻塞吗？**
6. **是否存在一个更直接推进 benchmark 的动作？**
7. **当前工作是在创造项目价值，还是只是在优化工具？**
8. **完成这一批以后，战略上的“下一大步”是什么？**

如果第 4、5、6 题答不清楚，默认应暂停局部扩展，回到战略地图重新排序。

## 9. 当前阶段为什么在做 Memory Atlas

当前的 Memory Atlas 不是为了证明“模型很懂锂电”，也不是为了建立第二套事实数据库。

它只解决一个非常具体的历史恢复问题：

> **互联网回搜旧历史很难，而高级模型可能记得大量普通搜索不会主动想到的 actor、旧称、机制、失败项目、合同结构和当时叙事。先把这些潜在目录系统性榨出来，再去逐条找证据，可以显著降低历史缺失。**

所以当前阶段必须最终走向：

```text
Memory Atlas 饱和
→ seal
→ 高级模型自证/搜索
→ Evidence task packet
→ 低级 Agent 深度取证
→ 原始资料归档
→ Reality / Expectation / Outcome
→ 第一套真实 historical replay
```

如果 Memory Atlas 长期不进入后半段，它就从“手段”变成了“新的牛角尖”。

## 10. 下一步“大方向”永远比当前 TODO 高一级

当前实时 TODO 由 `.longcycle/handoff/current.json` 保存，不在本文件复制。

但无论当前具体做到哪一步，大方向遵循：

```text
先证明一个行业历史能被完整重建
→ 证明当时预期能被真实保留
→ 证明 Reality vs Expectation vs Outcome 可以无后见之明地重放
→ 从今天开始持续归档，减少未来历史缺口
→ 把锂电中被真实数据验证过的 primitive 抽象成可复用方法
→ 迁移到第二个行业验证通用性
→ 用未来更强模型 vintage 不断回补旧历史
```

任何长期工程路线都应该能在这条链上找到位置。

## 11. 大方向的修改权限

战略航向不能被某个后续 Agent 因为“觉得另一种架构更酷”而静默改掉。

允许改变本文件的情况：

1. 用户明确改变产品方向；
2. 真实 benchmark 证明某个核心假设错误；
3. 有可审计的重大新证据表明路线需要调整。

修改必须：

- 保存旧版本于 Git 历史；
- 写 devlog 说明改变了什么、为什么；
- 更新 constitution / handoff 中受影响的长期承诺；
- 不允许把一次局部实现困难包装成战略转向。

## 12. 给未来 Agent 的一句话

> **不要因为你眼前拿着的是一颗螺丝，就忘了我们要造的是一艘能穿越几十年产业历史的船。先确认航向，再拧螺丝。**
